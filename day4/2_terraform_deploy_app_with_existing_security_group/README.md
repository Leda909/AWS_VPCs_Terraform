# Deploy the node.js App (no database, just homepage) via using Terraform

To test that the App runs home page:<br>
34.245.94.249
`http://<Public IP of App>`

Create a separate TF project
Use default VPC and subnet
Specify your app SG that lives inside the default VPC and subnet
Find how to run user data through terraform